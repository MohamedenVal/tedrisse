import { PaginatedCategory } from './paginated-category.model';

describe('PaginatedCategory', () => {
  it('should create an instance', () => {
    expect(new PaginatedCategory()).toBeTruthy();
  });
});
